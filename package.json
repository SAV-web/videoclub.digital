{
  "name": "videoclub-vite",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/generate-llms.mjs && node scripts/generate-sitemap.mjs && vite build",
    "build:worker": "npx esbuild cloudflare/worker.js --bundle --format=esm --outfile=cloudflare/dist/worker.bundle.js",
    "build:seo-css": "node scripts/build-seo-css.mjs",
    "build:edge": "node scripts/build-seo-css.mjs && npx esbuild cloudflare/worker.js --bundle --format=esm --outfile=cloudflare/dist/worker.bundle.js",
    "check": "tsc --noEmit",
    "preview": "vite preview",
    "preview:seo": "node scripts/preview-seo.mjs",
    "generate:llms": "node scripts/generate-llms.mjs",
    "test": "node --test --test-concurrency=1 --test-reporter=spec tests/*.test.mjs"
  },
  "devDependencies": {
    "typescript": "~5.9.3",
    "vite": "^6.0.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.112.2",
    "lru-cache": "^11.2.2"
  }
}
