{
  "name": "videoclub-vite",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "check": "tsc --noEmit",
    "preview": "vite preview",
    "test": "node --test --test-concurrency=1 tests/*.test.mjs"
  },

  "devDependencies": {
    "typescript": "~5.9.3",
    "vite": "^6.0.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.112.2",
    "lru-cache": "^11.2.2"
  }
}